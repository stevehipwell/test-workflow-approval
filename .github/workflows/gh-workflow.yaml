name: Test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |-
          set -euo pipefail

          echo "${{ toJSON(github) }}"

          echo "I have run..."
